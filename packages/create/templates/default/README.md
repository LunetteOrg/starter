# starter (default template)

The full-featured Lunette starter: monorepo (Turbo + pnpm), Biome, Drizzle,
testcontainers, design system, CI. This demo folder is a stand-in — the real
`templates/default/` would contain the entire current starter tree.
