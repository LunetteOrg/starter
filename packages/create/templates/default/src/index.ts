// Entry point for the default template.
// In the real spike this folder would hold the full starter monorepo
// (packages/, apps/, turbo.json, docs/adr, …) — imports use the `@starter/*`
// scope, which the CLI rewrites to `@<project-name>/*` on scaffold.
export {}
