# PDR-NNNN: Title

- Status: proposed | accepted | superseded by PDR-NNNN
- Date: YYYY-MM-DD
- Reviewers: <who signed off — client / product owner / …>

## Context

The product/user problem. What we know, what the reference material
(`reference/…`) tells us, what the constraint is. Why a call is needed now.

## Decision

What we chose, in active voice — the flow, the scope, the cut. Concrete enough
that a designer or engineer can act on it without asking.

## Rationale

Why this over the alternatives we considered. One line per rejected option is
enough.

## Consequences

- \+ What this unlocks.
- − What we give up or defer.

<!--
Renders in Storybook under `Product/` via a page that imports this file `?raw`.
Link the prototype and the "before" it replaces so the whole evolution reads on
one screen. See docs/guidances/product-decisions.md.
-->
