import { Meta, Markdown } from '@storybook/addon-docs/blocks'
import pdr from '../../../../docs/product/decisions/template.md?raw'

<Meta title="Product/Example" />

# Example decision page

A **live** decision page: the markdown below is the `docs/product/decisions/`
template, imported `?raw` and rendered here. Copy this file to
`src/Product/<Decision>.mdx`, point the import at your PDR, and drop the prototype
in. (The `product-decision` skill does both for you.)

<Markdown>{pdr}</Markdown>

## The explored future

Embed the interactive prototype here so stakeholders can *use* it next to the
rationale above:

<div
  style={{
    display: 'grid',
    placeItems: 'center',
    width: '100%',
    height: 320,
    border: '1px dashed var(--sb-border, #ccc)',
    borderRadius: 8,
    color: 'var(--sb-muted, #888)',
    fontFamily: 'sans-serif',
  }}
>
  {'<iframe src="…prototype…" /> goes here'}
</div>

## The "before"

Screenshots of the legacy flow from `reference/` go here — so one screen tells the
whole story: where we came from → what we explored → what we chose.
